import { Vector3 } from 'three'
import { GeometryProps } from '../../../../types/threeTypes'

export const snakeSCULL: GeometryProps = {
  position: new Vector3(0, 0, 0.25),
  'rotation-x': 0,
  'rotation-y': 0,
  'rotation-z': 54.98,
  scale: 0.4,
}
