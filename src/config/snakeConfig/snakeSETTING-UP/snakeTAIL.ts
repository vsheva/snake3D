import { Vector3 } from 'three'
import { GeometryProps } from '../../../types/threeTypes'

export const snakeTAIL: GeometryProps = {
  position: new Vector3(0, -0.1, 0.05),
  'rotation-x': 0,
  'rotation-y': 0,
  'rotation-z': 11,
  scale: 0.8,
}
